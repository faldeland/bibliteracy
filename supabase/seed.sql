-- Local seed data, applied automatically by `supabase db reset`.
-- Keep this file safe to run against an empty local database. Production data
-- should NEVER be seeded from here — only deterministic dev fixtures.

-- (intentionally empty — add fixtures here when useful, e.g. demo profiles
-- and dots so the grid has something to render in local dev)
